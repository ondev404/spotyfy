const{spoty,valid,track}=require("./_lib");
module.exports=async(req,res)=>{try{if(req.method!=="POST")return res.status(405).json({success:false,message:"Method tidak diizinkan"});let{url}=req.body||{};if(!url)return res.status(400).json({success:false,message:"URL playlist wajib diisi"});if(!valid(url))return res.status(400).json({success:false,message:"URL Spotify tidak valid"});let d=await spoty("/api/spotify/playlist",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({url})});if(!d.post)throw Error("Playlist tidak ditemukan");let p=d.post;res.json({success:true,data:{judul:p.name,owner:p.owner,total_lagu:p.total_tracks,thumbnail:p.image||null,daftar_lagu:(p.tracks||[]).map(track)}})}catch(e){console.error(e);res.status(500).json({success:false,message:e.message})}};